/**
 * Custom MCP tools for Coffee Agent.
 * These will be registered via createSdkMcpServer() in future iterations.
 * 
 * Planned tools:
 * - send_message: Agent can proactively send messages during execution
 * - schedule_task: Agent can create scheduled tasks
 * - memory_append: Agent can write to persistent memory
 */

// Placeholder — MCP tools will be implemented in P1 phase
export const mcpToolDefinitions = {
  // Will use createSdkMcpServer() from SDK
}
